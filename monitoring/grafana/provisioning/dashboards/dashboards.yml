apiVersion: 1

providers:
  - name: 'FIAP X Dashboards'
    orgId: 1
    folder: 'FIAP X'
    folderUid: 'fiapx'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
